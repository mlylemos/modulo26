function multiplicar(a: number, b: number): number {
  return a * b;
}

function saudacao(nome: string): string {
    return "Olá " + nome;
}

console.log("Resultado da multiplicação:", multiplicar(7, 4));
console.log("Mensagem de saudação:", saudacao("Mundo"));